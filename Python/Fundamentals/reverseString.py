# ---------------------------------------------------------------------------
# REVERSE A STRING   (LeetCode 344)
# ---------------------------------------------------------------------------
# Problem:
#   Given a string, return it reversed.
#
# Examples:
#   "hello"  ->  "olleh"
#   "DSA"    ->  "ASD"
#   "a"      ->  "a"
#   ""       ->  ""        (empty string stays empty)
#
# Constraints:
#   0 <= len(s) <= 1000
#
# CHALLENGE: do it WITHOUT the slice trick s[::-1] first.
#   Try a loop (build the result character by character) OR the two-pointer
#   technique (swap the ends and move inward). AFTER you get it working,
#   learn the one-line s[::-1] way and compare.
#
# Concept this builds: string indexing, building results, two-pointer thinking.
# ---------------------------------------------------------------------------

# Write your solution below 👇
