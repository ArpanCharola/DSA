# ---------------------------------------------------------------------------
# CHECK PALINDROME
# ---------------------------------------------------------------------------
# Problem:
#   A palindrome reads the same forwards and backwards.
#   Given a string, return True if it is a palindrome, otherwise False.
#
# Examples:
#   "racecar"  ->  True
#   "madam"    ->  True
#   "hello"    ->  False
#   "a"        ->  True
#
# Constraints:
#   1 <= len(s) <= 1000
#   For now assume the input is all lowercase with no spaces.
#   (Stretch: handle "Race Car" by ignoring case and spaces.)
#
# Hint: the two-pointer technique shines here — compare the first and last
# characters, then move inward. The moment two differ, it's not a palindrome.
#
# Concept this builds: two-pointer technique, boolean logic, early return.
# ---------------------------------------------------------------------------

# Write your solution below 👇
